# 도로 손상 통합 관리 시스템 디자인 가이드 (Road Damage Detection System Design Guide)

본 문서는 '도로 손상 통합 관리 시스템'의 디자인 원칙과 시각화 전략을 정리한 가이드라인입니다.

---

## 1. 디자인 원칙 (Design Principles)
*   **고신뢰성**: 공공 인프라 관리를 위한 정확한 수치 데이터 전달에 집중합니다.
*   **데이터 중심**: 복잡한 수만 개의 데이터를 시각적으로 그룹화하여 핵심 인사이트를 즉시 포착하게 합니다.
*   **반응성**: 다양한 줌 레벨과 기기 환경에서도 성능 저하 없이 최적의 시각화 모드를 제공합니다.

---

## 2. GIS 및 공간 분석 전략 (GIS & Spatial Strategy)

### 2.1. 줌 레벨별 하이브리드 시각화
대규모 데이터(수백만 건 이상)를 효율적으로 시각화하기 위해 Deck.gl과 Google Maps를 결합합니다.
*   **광역 뷰 (Zoom 0-9) - Deck.gl 히트맵(Heatmap) 모드**: GPU 가속을 통한 고정밀 밀도 표현. 데이터가 적은 곳은 파랑, 밀집된 곳은 빨강으로 부드러운 그라데이션 형성.
*   **상세 뷰 (Zoom 10-22) - 개별 마커(Marker) 모드**: 정확한 지점에 핀을 표시하고 클릭 시 상세 이미지 및 손상 부위 BBox 정보 제공.

### 2.2. 정밀 공간 필터링 (GeoTools)
데이터의 무결성을 높이기 위해 백엔드에서 **GeoTools** 엔진을 활용합니다.
*   **육지 경계 검증**: `ne_110m_land.shp` 데이터를 메모리 내 공간 인덱스(STRtree)로 로드하여, 바다 위에 생성된 가상 마커나 노이즈 데이터를 API 레벨에서 자동으로 필터링합니다.

---

## 3. 상세 분석 디자인 (Detailed Analysis Design)

### 3.1. Bounding Box (BBox) 시각화
YOLO 형식의 정규화된 좌표(xCenter, yCenter, Width, Height)를 이미지 픽셀 영역과 1:1로 매칭합니다.
*   **픽셀 밀착 렌더링**: 이미지를 감싸는 컨테이너를 이미지 크기에 소수점 단위로 밀착시켜 박스 위치의 왜곡을 방지합니다.
*   **인터랙티브 레이어**: 박스 위에 마우스 호버 시 손상 코드(`D00`, `D10` 등)를 강조 표시합니다.

### 3.2. 로컬 데이터셋 스트리밍
대용량 이미지 파일을 별도의 스토리지 업로드 없이 로컬 파일 시스템에서 직접 스트리밍하여 프라이버시와 속도를 동시에 확보합니다.

---

## 4. UI 컴포넌트 가이드

### 4.1. 색상 시스템 (Color Palette)
*   **Primary (Blue)**: `#0058be` - 시스템 주조색, 데이터 신뢰 및 안정성 상징.
*   **Secondary (Grey/Blue)**: `#545f73` - 보조 정보 및 배경 보조색.
*   **Danger (Red)**: `#ba1a1a` - 심각한 파손(D40) 및 BBox 강조색.
*   **Success (Green)**: `#22c55e` - 신규 감지 및 정상 상태 표시.

### 4.2. 데이터 관리 테이블
*   **인터랙티브 소팅**: ID, 위도, 경도 등 핵심 컬럼에 대해 오름차순/내림차순 정렬 기능을 제공하여 데이터 관리 효율성을 극대화합니다.
*   **고밀도 레이아웃**: 많은 정보를 한눈에 볼 수 있도록 폰트 크기와 패딩을 최적화한 데이터 테이블을 사용합니다.
