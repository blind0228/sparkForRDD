# 도로 손상 통합 관리 시스템 디자인 가이드 (Road Damage Detection System Design Guide)

본 문서는 **Stitch MCP**를 통해 정의된 '도로 손상 통합 관리 시스템'의 디자인 시스템 규칙을 정리한 문서입니다. 본 시스템은 공공 안전 및 도로 인프라 유지보수를 담당하는 지방자치단체 운영자와 엔지니어가 사용하는 고신뢰성, 데이터 중심 환경을 위해 설계되었습니다.

---

## 1. 브랜드 및 스타일 (Brand & Style)

*   **브랜드 아이덴티티**: **권위적이고 정밀하며 체계적인(Authoritative, Precise, Systematic)** 이미지 구축. 운영자에게 신뢰감을 전달합니다.
*   **비주얼 스타일**: **기업형 및 현대적(Corporate / Modern)**인 스타일을 지향하며, 정보 밀도를 극대화하면서도 시각적 명확성을 해치지 않는 기능적인 SaaS 에이전트 인터페이스를 제공합니다. 불필요한 장식적 요소를 배제하고 정돈된 라인과 훈련된 색상 레이아웃을 사용합니다.

---

## 2. 색상 규칙 (Colors)

전문적 신뢰성과 인프라 관리 특성을 직관적으로 전달할 수 있도록 구성된 색상 팔레트입니다.

### 2.1. 핵심 색상 (Core Palette)
*   **주 색상 (Primary - Corporate Blue)**: `#3B82F6` (Stitch 테마 기본값 `#0058be`)
    *   기본 버튼, 대화형(Interactive) 요소, 주요 액션 호출 등에 사용하여 명확한 행동 유도를 담당합니다.
*   **보조 색상 (Secondary/Base - Deep Navy)**: `#1E293B` (Stitch 테마 기본값 `#545f73`)
    *   어플리케이션의 중심을 잡기 위해 주로 사용됩니다. 내비게이션 사이드바 및 탑 헤더의 배경으로 쓰입니다.
*   **배경 색상 (Background & Surfaces)**:
    *   배경 (Background): `#f9f9ff` (눈의 피로를 최소화하는 부드러운 오프화이트/그레이 레이어)
    *   표면 (Surface): `#f9f9ff`
    *   표면 음영 (Surface Dim): `#d8d9e3`
    *   표면 컨테이너 (Surface Container): `#ecedf7`
    *   표면 대비 텍스트 (On-Surface): `#191b23`
    *   테두리선 (Outline): `#727785` / `#c2c6d6`

### 2.2. 의미적 색상 (Semantic Colors)
도로 손상 유형(Severity) 및 상태를 시각적으로 즉시 분류하기 위해 아래 색상을 엄격히 제한하여 사용합니다.
*   **심각 (Critical - D00/D10)**: **빨간색 (`#ba1a1a`)**
    *   긴급 보수가 필요하거나 즉각적인 조치가 요구되는 위험 요소에만 독점적으로 사용합니다.
*   **경고 (Warning)**: **노란색 (`#f59e0b` 등)**
    *   보수 검토 대기 중이거나 보통 수준의 도로 균열/손상 상태를 나타냅니다.
*   **성공/정상 (Success)**: **초록색 (`#10b981` 등)**
    *   보수 조치가 완료되었거나 안전이 확보된 상태를 나타냅니다.
*   **중립 (Neutral)**: **회색조 (Slate Grays)**
    *   테두리, 보조 텍스트, 배경 카드 적층 구조 등에 사용하여 장시간 사용 시 발생할 수 있는 시각적 피로를 예방합니다.

---

## 3. 타이포그래피 규칙 (Typography)

구조적인 헤더에는 현대적이고 기하학적인 명확성을 주는 **Manrope**를 사용하고, 데이터 밀도가 높은 테이블 및 텍스트 본문 영역에는 가독성이 뛰어난 **Inter** 폰트를 조합하여 사용합니다.

*   **한글 구현 규칙**: 한글 문자가 밀집되어 뭉쳐 보이지 않도록 라틴(영문) 디폴트 값 대비 **줄 간격(Line Height)을 10~15% 정도 추가로 확보**합니다.

| 분류 (Token) | 폰트 패밀리 (Font Family) | 크기 (Size) | 굵기 (Weight) | 줄 간격 (Line Height) | 자간 (Letter Spacing) | 용도 |
| :--- | :--- | :--- | :--- | :--- | :--- | :--- |
| **display-lg** | Manrope | 32px | Bold (700) | 40px | -0.02em | 대화면 타이틀 및 주요 메트릭 강조 |
| **display-md** | Manrope | 24px | Bold (700) | 32px | -0.01em | 메인 섹션 제목 |
| **headline-sm** | Manrope | 20px | Semi-Bold (600) | 28px | 기본 | 대시보드 위젯/카드 제목 |
| **body-lg** | Inter | 16px | Regular (400) | 24px | 기본 | 상세 본문 텍스트 |
| **body-md** | Inter | 14px | Regular (400) | 20px | 기본 | 일반 설명 및 단락 본문 |
| **body-sm** | Inter | 13px | Regular (400) | 18px | 기본 | 부가 설명, 테이블 서브 데이터 |
| **label-bold** | Inter | 12px | Semi-Bold (600) | 16px | 0.02em | 테이블 헤더 라벨, 폼 인풋 레이블 |
| **data-mono** | Inter | 14px | Medium (500) | 20px | -0.01em | GPS 좌표, ID, 타임스탬프 등 정밀 데이터 |

---

## 4. 레이아웃 및 간격 (Layout & Spacing)

다양한 모니터 환경에서 원활한 모니터링이 가능하도록 **고정-유동 하이브리드 그리드(Fixed-Fluid Hybrid Grid)**를 채택합니다.

*   **기본 구조**:
    *   **좌측 내비게이션 사이드바**: `260px` 고정 너비
    *   **우측 메인 콘텐츠 영역**: 브라우저 폭에 반응하는 `12열 유동 그리드 (12-Column Fluid Grid)`
*   **기준 스페이싱 (4px Baseline Grid)**:
    컴포넌트의 배치와 정렬의 일관성을 위해 4의 배수 단위를 그리드 시스템의 스페이싱 룰로 적용합니다.
    *   `base`: 4px
    *   `xs`: 8px
    *   `sm`: 12px
    *   `md`: 16px
    *   `lg`: 24px
    *   `xl`: 32px
    *   `gutter`: 20px (위젯 간의 기본 간격)
*   **디바이스별 마진 대응 (Responsive Breakpoints)**:
    *   **데스크톱 (Desktop)**: 외곽 여백(Outer Margin) `24px` / 위젯 간격(Gutter) `20px`
    *   **태블릿 (Tablet)**: 외곽 여백 `16px` / 위젯이 2열 배치로 자동 재정렬
    *   **모바일 (Mobile)**: 외곽 여백 `12px` / 모든 요소가 단일 컬럼으로 세로 정렬 및 단순화된 데이터 뷰 제공

---

## 5. 고도 및 깊이 규칙 (Elevation & Depth)

컴포넌트의 중요성과 인터랙션 상태를 시각적인 적층(Z-Index) 구조로 인지할 수 있도록 입체감을 부여합니다.

*   **Level 0 (기본 배경)**: `#F8FAFC` (또는 `#f9f9ff`)
    *   데이터 테이블과 지도 컴포넌트가 눈에 띄도록 화면 전체의 베이스가 되는 반무광 그레이 배경입니다.
*   **Level 1 (카드 및 위젯)**:
    *   개별 차트 카드, 통계 요약 카드 등 기본 컨테이너 표면에 적용됩니다.
    *   순수 흰색 배경(`#FFFFFF`) + 아주 엷고 넓게 퍼지는 그림자 (`0px 1px 3px rgba(0, 0, 0, 0.05)`) + 연한 1px 경계선 테두리 (`#E2E8F0` 또는 `#c2c6d6`).
*   **Level 2 (드롭다운 및 팝오버)**:
    *   콤보박스 선택 메뉴, 지도 위 호버 카드 등 공중에 떠 있는 요소에 사용합니다.
    *   조금 더 명확하고 뚜렷한 그림자 효과 (`0px 10px 15px -3px rgba(0, 0, 0, 0.1)`)를 적용합니다.
*   **Level 3 (모달 및 확인 창)**:
    *   전체 화면 위에 오버레이 되는 모달 창에 적용합니다.
    *   배경을 어둡게 하는 백드롭 블러(Backdrop Blur, Navy 계열 투명도 40% 적용) 처리를 병행하여 시선을 집중시킵니다.

---

## 6. 형태 및 둥글기 규칙 (Shapes & Rounding)

너무 딱딱하지 않으면서도 소프트웨어의 정밀함과 현대적인 SaaS 감각을 주기 위해 모서리 곡률(Border-Radius)을 일관적으로 통제합니다.

*   **표준 인터랙션 요소 (Buttons, Input Fields, Small Cards)**: `0.5rem (8px)`
*   **대형 컨테이너 (Dashboard Widgets, Main Content Panels)**: `1rem (16px)`
*   **상태 표시 배지 (Status Badges/Chips)**: 완전 둥근 캡슐 형태 (`pill-shaped / full round`)로 처리하여 대화형 버튼과 형태적으로 즉시 분리되도록 합니다.

---

## 7. 주요 컴포넌트 디자인 규칙 (Components Guide)

### 7.1. 버튼 (Buttons)
*   **기본 버튼 (Primary)**: `#3B82F6` (Corporate Blue) 단색 채우기 + 흰색 텍스트. 상단 경계선에 미세한 1px 인라인 하이라이트 처리를 하여 클릭하고 싶은 입체감 제공.
*   **보조 버튼 (Secondary)**: 테두리선 및 텍스트에 슬레이트 그레이 계열을 사용하는 아웃라인 형태.

### 7.2. 상태 배지 (Status Badges)
*   정보 구분을 명확히 하기 위해 10%의 저농도 배경색을 칠하고, 텍스트는 100% 농도의 진한 동일 계열 컬러를 조합합니다.
    *   *예시 (심각 - D00/D10)*: `#FEE2E2` (빨간색 10%) 배경 + `#991B1B` (빨간색 100%) 볼드 텍스트 적용.

### 7.3. 데이터 테이블 (Data Tables)
*   대량의 도로 손상 정보 검색에 최적화되도록 구성합니다.
*   상단 헤더 스티키(Sticky) 고정.
*   가독성을 돕기 위해 행별로 미세한 Zebra-striping(번갈아 나타나는 배경음영) 적용.
*   마우스 커서 호버 시 해당 로우(Row)가 아주 옅은 파란색 틴트로 변경되는 활성 상태 효과 제공.
*   패딩을 컴팩트하게 조율하여 표준 랩톱 모니터 기준 최소 15개 이상의 정보 행이 한 화면에 노출되도록 보장합니다.

### 7.4. 입력 필드 (Input Fields)
*   기본 상태: 순수 흰색 배경 + 1px 경계선 (`#CBD5E1`).
*   포커스(Focus) 상태: 경계선이 주 색상인 `Corporate Blue`로 전환되며 `3px` 너비의 부드러운 아웃라인 글로우(Glow) 효과 적용.
*   레이블: 입력 칸 위쪽에 `label-bold` 규칙으로 제목을 선언하여 안정적인 배치를 유지합니다.

### 7.5. 지도 오버레이 카드 (Map Overlay GIS Cards)
*   지도 위에 오버레이 되는 요약 카드들은 반투명 배경 처리를 적용하고 Level 2 입체 효과를 가집니다. 손상 ID, 손상 유형(D00~D40), 우선순위 정보를 포함하며 정밀 보고서 페이지로 직접 이동할 수 있는 바로가기 링크를 제공합니다.

### 7.6. 손상 상세 모달 (GIS Detail Modal)
*   마커 클릭 시 활성화되며, Level 3 고도 규칙을 따릅니다.
*   **이미지 영역**: 원본 이미지를 배경으로 사용하며, 그 위에 SVG 오버레이를 통해 바운딩 박스를 렌더링합니다.
*   **바운딩 박스 (Bounding Box)**: 
    *   YOLO 데이터(xCenter, yCenter, width, height)를 기반으로 상대 좌표 연산을 수행하여 이미지 크기에 맞춰 동적으로 위치를 조정합니다.
    *   심각도에 따른 테두리 색상(예: D00/D10은 `#ba1a1a`)과 반투명 채우기 색상을 적용하여 시인성을 확보합니다.
    *   박스 상단에는 손상 코드(D00 등) 라벨을 표시합니다.
*   **정보 영역**: 우측 또는 하단에 손상된 위치의 GPS 정보, 분석된 라벨 리스트, 발생 시간 등의 메타데이터를 정렬된 리스트 형태로 제공합니다.

---

## 8. GIS 시각화 전략 (GIS Visualization Strategy)

대규모 데이터(수백만 건 이상)를 효율적으로 시각화하기 위해 줌 레벨에 따른 하이브리드 전략을 채택합니다.

### 8.1. 줌 레벨별 시각화 모드
*   **광역 뷰 (Zoom 0-9) - 히트맵(Heatmap) 모드**:
    *   개별 마커 대신 데이터 밀도를 색상 농도로 표현합니다.
    *   브라우저 렌더링 부하를 최소화하고 국가/대륙 단위의 분포를 직관적으로 파악합니다.
*   **지역 뷰 (Zoom 10-15) - 클러스터(Cluster) 모드**:
    *   그리드 단위로 집계된 숫자 마커를 표시합니다.
    *   도시/구 단위에서 구체적인 발생 건수를 인지할 수 있도록 돕습니다.
*   **상세 뷰 (Zoom 16-22) - 개별 마커(Marker) 모드**:
    *   개별 도로 손상 지점을 정확한 위치에 표시합니다.
    *   마커 클릭 시 상세 정보 및 원본 사진 조회가 가능합니다.

### 8.2. 성능 최적화 원칙
*   **뷰포트 필터링**: 사용자가 보고 있는 현재 화면 영역(Bounding Box) 내의 데이터만 서버에 요청합니다.
*   **서버 측 집계**: 지도가 축소된 상태에서는 서버(PostGIS)에서 미리 집계된 데이터만 전송하여 네트워크 대역폭과 클라이언트 메모리를 보호합니다.
